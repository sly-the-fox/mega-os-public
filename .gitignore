# Secrets and credentials
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
sessions.json
chat-log.jsonl

# Runtime
node_modules
__pycache__
.venv
*.pyc
.next
out
build
coverage

# Test / dev artifacts
products/**/.coverage
products/**/.hypothesis/
products/**/.dockeeper.db

# Temp files
.send-telegram.sh
.telegram-tmp.txt

# OS / IDE
.DS_Store
Thumbs.db
.vscode/settings.json
.idea/

# Generated deliverables
deliverables/*.pdf
deliverables/*.docx

# Style samples — tracked locally, excluded from public sync via sync-to-public.sh

# Claude Code local
.claude/settings.local.json
.claude/worktrees/
.claude/scheduled_tasks.lock
.mcp.json

# Playwright
.wrangler/

# Nested repos (upstream PR forks)
products/sigil/public-repo/servers/

# Private / proprietary (currently unused — Codex moved public 2026-03-08)
# private/

# Trace data (ephemeral operational data, not versioned)
core/history/traces/*.jsonl
core/history/traces/archive/

# Memory index (rebuilt by cron)
engineering/memory.db
